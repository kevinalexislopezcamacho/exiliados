PORT=4001
FRONTEND_URL=http://localhost:3000

# Turso (libSQL): crea una base gratis en https://turso.tech y pega aca la
# Database URL y un Auth Token. Reemplaza al archivo SQLite local (data.db)
# para que la base persista en Vercel (sin disco local entre invocaciones).
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
